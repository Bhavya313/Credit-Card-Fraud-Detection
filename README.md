# Credit-Card-Fraud-Detection
End-to-end credit card fraud detection system built using Random Forest and SMOTE to handle class imbalance. The project includes data preprocessing, model training, evaluation, threshold tuning, and deployment as a FastAPI service, fully containerized using Docker for scalable and portable deployment.
